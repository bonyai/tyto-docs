---
title: Usage
description: Drive nests, sessions, files, previews, snapshots, and holds from TypeScript
---

```ts
import { Tyto } from "@bonya-ai/tyto";

const tyto = new Tyto();

// Who am I?
const me = await tyto.me();
console.log(me.email);

// Create a nest
const nest = await tyto.create({ name: "my-nest", template: "ubuntu-24-dev" });

// Upload / download a file or directory
await nest.put("./hello.txt", "hello.txt");
await nest.get("hello.txt", "./hello.downloaded.txt");

// Create a session and attach via a signed grant + WebSocket
const session = await nest.createSession({ tty: false, argv: ["bash", "-lc", "echo hi"] });
const ws = await session.attach();
ws.on("message", (d) => console.log(String(d)));

// Open an interactive shell / one-off TTY exec (also grant + WebSocket under the hood)
const console_ = await nest.console();
const exec_ = await nest.execTty({ argv: ["htop"] });

// Create a preview
const preview = await nest.createPreview({ port: 3000, auth: "private" });
console.log(preview.url);

// Snapshot, fork, restore
const snap = await nest.createSnapshot({ name: "v1" });
const fork = await nest.fork({ name: "my-fork" });
await nest.deleteSnapshot(snap.id!);
// await nest.restore(snap.id!);

// Keepalive hold
await nest.holds.put("ci", { ttl: "30m", reason: "CI job" });
await nest.holds.heartbeat("ci", { ttl: "30m" });
await nest.holds.delete("ci");

// Lifecycle
await nest.stop();
await nest.start();
await nest.wake({ reason: "wakeup" });
await nest.delete();
```

## Resources

| Resource | Description |
|---|---|
| `tyto.create()` | Create a nest |
| `tyto.nests` | Create / list / get nests |
| `tyto.previews` | Inspect / revoke previews by ID |
| `tyto.auth` | CLI browser auth flow |
| `nest.put(local, remote)` | Upload a file or directory |
| `nest.get(remote, local)` | Download a file or directory |
| `nest.createSession()` | Create a managed session |
| `nest.createPreview()` | Create a preview URL |
| `nest.createSnapshot()` | Create a snapshot |
| `nest.deleteSnapshot()` | Delete a snapshot |
| `nest.fs` | Low-level file upload / download |
| `nest.sessions` | Managed sessions (create / list) |
| `nest.previews` | Previews scoped to the nest |
| `nest.snapshots` | Snapshots + fork/restore |
| `nest.holds` | Keepalive holds |
| `session.attach()` | Prepare a grant and stream a session's I/O over WebSocket |
| `nest.console()` | Prepare a grant and open an interactive shell PTY |
| `nest.execTty()` | Prepare a grant and run a one-off TTY command |

## Examples

The SDK repo ships runnable examples covering these flows:

```bash
npx tsx examples/quickstart.ts
npx tsx examples/files.ts
npx tsx examples/preview.ts
npx tsx examples/snapshot-fork.ts
npx tsx examples/websocket-exec.ts
```
